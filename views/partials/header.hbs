<header>
  <!-- navigation -->
  <nav class='navbar navbar-light bg-light'>
    <div class='container'>
      <div>
        <a class='navbar-brand' href='/'>
          <img
            src='https://assets-lighthouse.s3.amazonaws.com/uploads/image/file/6227/restaurant-list-logo.png'
            width='30'
            height='30'
            class='d-inline-block align-top'
            alt='我的餐廳清單'
          />
          我的餐廳清單
        </a>
        {{#if isAuthenticated}}
          <a
            class='d-inline-block align-top'
            style='text-decoration:none;font-size:1.2rem;color:#267b98'
            href='/'
          >歡迎回來！{{user.name}}</a>
        {{/if}}
      </div>
      {{#if isAuthenticated}}
      <div>
        <a
          href='/restaurants/new'
          class='btn btn-outline-secondary'
        >新增餐廳</a>
        <a href='/users/logout' class='btn btn-outline-success'>登出</a>
      </div>
      {{/if}}
    </div>
  </nav>
  {{#if isAuthenticated}}
    <div class='banner'></div>
  {{/if}}
</header>